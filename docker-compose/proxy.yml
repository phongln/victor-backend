version: '3'
services:
  proxy-nginx:
    container_name: proxy-nginx
    image: jwilder/nginx-proxy:alpine
    ports:
      - '80:80/tcp'
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
    networks:
      - backend-net

networks:
  backend-net:
    external: true